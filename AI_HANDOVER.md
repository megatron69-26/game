# TÀI LIỆU CHUYỂN GIAO NGỮ CẢNH DỰ ÁN (AI HANDOVER CONTEXT)

**Gửi tới AI Agent mới:** Đây là tài liệu tóm tắt toàn bộ lịch sử, tiến độ và quy tắc của dự án để bạn có thể nắm bắt ngay lập tức và phối hợp làm việc cùng tôi (AI hiện tại) và User (Human).

---

## 1. THÔNG TIN CHUNG VỀ DỰ ÁN
*   **Tên dự án:** Tệ Nạn
*   **Engine:** Godot 4.x (GDScript)
*   **Thể loại:** 3D Tabletop Roguelike. 
*   **Concept:** Là sự lai tạo giữa **Buckshot Roulette** (Bối cảnh 3D rùng rợn, góc nhìn thứ nhất, UI vật lý/Diegetic), **Balatro** (Tính điểm sát thương bằng tay bài Poker: Base x Mult), và **Slay The Spire** (Tiến trình leo tháp, bản đồ nhánh).
*   **Thư mục làm việc:**
    *   Repo Git: `C:\Users\khoid\game\`
    *   Godot Project: `C:\Users\khoid\OneDrive\Documents\tệ-nạn\`

## 2. QUY TẮC LÀM VIỆC (PAIR-PROGRAMMING WORKFLOW)
Đây là thỏa thuận cốt lõi không được phá vỡ:
*   **👨‍🎨 Lãnh đạo Mỹ thuật / Level Designer (Human/User):** Chịu trách nhiệm 100% thao tác trên màn hình Godot Editor. Sắp xếp model 3D, kéo thả node, gắn vật liệu (texture), đúc Scene.
*   **💻 Kỹ sư Logic / Lead Coder (AI):** Chịu trách nhiệm 100% về Code (GDScript). Không đưa ra các toạ độ 3D bừa bãi (như `x: 1.5, y: 2.0`) vì AI không có mắt để nhìn Viewport. Chỉ cung cấp script và hướng dẫn User cách gắn script vào Node.

## 3. TÓM TẮT LỊCH SỬ PHÁT TRIỂN (NHỮNG GÌ ĐÃ DIỄN RA)
1. **Trích xuất tài sản (Asset Ripping):** User đã dùng Godot RE Tools bóc thành công toàn bộ model 3D (`.glb`), âm thanh, và texture từ game gốc Buckshot Roulette.
2. **Dọn dẹp & Import:** AI đã dùng script PowerShell tự động copy toàn bộ thư mục `models`, `textures`, `audio`, `GodotRetro` vào project mới, đồng thời **xóa sạch các file `.import` cũ** để tránh lỗi dính script hỏng của game gốc.
3. **Chốt Kiến trúc (Roadmap):** Đã chốt file `ROADMAP.md` trên Git, chia game thành 3 Scene chính: `MainMenu.tscn`, `MapShop.tscn`, và `BanChoi.tscn` (Combat).
4. **Xây dựng Bàn chơi (BanChoi.tscn):** User đã import thành công cái bàn, Dealer (`smoking dude`), và cọc bài (`deck_of_cards.glb` - convert từ fbx sang qua Blender). Mọi thứ đã đặt lên bàn.
5. **Đúc Lá bài 3D:** User đã tạo xong Scene `Card3D.tscn` với cấu trúc chuẩn: `Area3D` -> `MeshInstance3D` -> `CollisionShape3D`.

## 4. TÌNH TRẠNG CODE HIỆN TẠI (MỚI NHẤT)
AI vừa hoàn thành và đẩy 2 file Script nòng cốt vào project:
*   **`Card3D.gd`:** Xử lý hiệu ứng lơ lửng. Khi chuột lướt qua (`mouse_entered`), lá bài dùng `Tween/Lerp` nhô lên trục Y. Có nhận sự kiện click (`_input_event`).
*   **`GameManager.gd`:** Nằm ở Node gốc `combat`. Đã thiết lập `@export var card_template` để nhận file `Card3D.tscn`. Đã viết hàm `chia_bai()` kích hoạt bằng phím **Spacebar**. Hàm này dùng `Tween` để phóng 5 lá bài từ toạ độ `DeckSpawnPoint` bay lơ lửng dàn hàng ngang ra trước mặt người chơi.

## 5. MỤC TIÊU TIẾP THEO (TBD)
*   **Đối với User:** Nhấn F5 chạy thử hàm phóng 5 lá bài bằng phím Spacebar và nghiệm thu.
*   **Đối với AI Mới & AI Cũ:** Tạm thời chưa có phân chia công việc cụ thể (User sẽ chỉ định sau). Trách nhiệm hiện tại của 2 AI là đọc tài liệu này, hiểu tư duy thiết kế 3D Diegetic của game, và sẵn sàng nhận lệnh để code các hệ thống tiếp theo (VD: PokerLogic, GunManager, DealerAI).

---
*Generated by Antigravity AI - 2026-09-06*
